				$(function() {
					//加载头部导航区
					//$('#container_top').load('top_nav.html');
					//加载左侧导航区
					//$('#container_centre_left').load('left_nav.html');
					//加载右侧聊天区
					//$('#container_centre_right').load('talk.html');
					//加载文件上传模态框
					$('#fileupload').load('fileinput.html');
					//视频左右两边高度跟随视频变化
					/*$("#center_video").resize(function() {
						$("#container_centre_left").height($(window).height());
						$("#container_centre_right").height($("#center_video").height());
					});
					window.onresize = function() {
						$("#container_centre_left").height($(window).height());
						$("#container_centre_right").height($("#center_video").outerHeight());
					}*/
				});